<h1>Books#index</h1>
<p><%= "こんにちは、#{current_user.name}さん" %></p>
<% @books.each do |book| %>
  <%= link_to book.name, user_book_words_path(current_user.uid, book) %>
<% end %>
<%= link_to 'logout', destroy_user_session_path, method: :delete %>
